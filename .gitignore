__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.xapk
*.xapk.zip
*.apk
.DS_Store
*.log
.env

# Local scratch: patched copy for live testing + protocol capture tooling
DEPLOY/

# Coverage artefacts (the gate reads coverage.json in CI, then discards it)
coverage.json
.coverage
htmlcov/
